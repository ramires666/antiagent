# MCP is registered once at user level by `antiagent-codex-install`.
# A project-level command would override that absolute launcher path and make
# startup depend on the parent process PATH, so this trusted checkout keeps no
# duplicate server registration.
